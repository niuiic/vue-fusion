# hooks

## 用法

1. 引入包。

`package.json`

```json
{
  "dependencies": {
    "hooks": "workspace:*",
    "result": "workspace:*",
    "utils": "workspace:*"
    "types": "workspace:*"
  }
}
```

2. 使用hooks。
