{
  "name": "components",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "stylelint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
